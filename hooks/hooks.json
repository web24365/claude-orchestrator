{
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "agent",
            "agent": "moai-orchestrator",
            "prompt": "Check if Ralph Engine completed a task (detected <promise>DONE</promise>) and update spec-status.json accordingly"
          }
        ]
      }
    ]
  }
}
